import GalleryManagerClient from './GalleryManagerClient';

export const metadata = {
  title: "Supper Club Admin | Gallery Manager",
};

export default function AdminGalleryPage() {
  return <GalleryManagerClient />;
}
